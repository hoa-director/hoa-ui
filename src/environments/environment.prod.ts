export const environment = {
  production: true,
  apiUrl: 'https://hoa-api-production.herokuapp.com/',
  // apiUrl: 'https://hoa-director.herokuapp.com', // -- Nate added, but did not seem to fix cors errors.
};
